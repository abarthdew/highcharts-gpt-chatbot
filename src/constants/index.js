export const DEFAULT_SYSTEM_MESSAGE = 'Use just one JSON markdown code block, Highcharts/Vue2, data object named `chartOptions` only. Do not include any html or import code. But you can explain something using plain text only.\n';
export const DEFAULT_USER_MESSAGE = '';
export const DEFAULT_DELAY_SECONDS = 1;
export const DEFAULT_EXPECTED_MESSAGES = 5;
