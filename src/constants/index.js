export const DEFAULT_SYSTEM_MESSAGE = 'Provide the chartOptions data required to create a graph using Highcharts in JSON format only. \n';
export const DEFAULT_USER_MESSAGE = '';
export const DEFAULT_DELAY_SECONDS = 1;
export const DEFAULT_EXPECTED_MESSAGES = 5;
